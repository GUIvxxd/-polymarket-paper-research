# Optional credentials for legacy/manual X API research probes only.
# The currently scheduled xtracker paper watchdog does not require these values.
# Leave blank unless an operator explicitly runs the optional probe scripts.
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# No Polymarket wallet, private key, signing key, or live-order credential belongs
# in this project. Do not add such variables to this template.
